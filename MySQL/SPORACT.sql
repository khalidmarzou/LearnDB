#create database SPORACT;
#use SPORACT;
#CREATE TABLE PROPOSER (nums int, numc int);
#INSERT INTO centre (numc, nomc, villec, countic)
#	value(22, "rien","kech", 35.21)
#	VALUE("TENNIS"),("FOOTBALL");
#select * from centre where villec="casablanca";
#SELECT * FROM centre, adherent, sport where numc > "15";
#create database SPORACT_2;
use SPORACT_2;
#create table sport (noms varchar(4));
#insert into sport (noms)
#	value("foot");
#SHOW create database sporact_2;
create table adherant (noma varchar(5),numa int(3),villea varchar(4),primary key (numa));